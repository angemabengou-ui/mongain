const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const count = await prisma.user.count({ where: { pushToken: { not: null } } }); console.log('Users with pushTokens:', count); const user = await prisma.user.findFirst({ where: { pushToken: { not: null } } }); console.log(user ? user.pushToken : 'None found'); } main().finally(() => prisma.$disconnect());
