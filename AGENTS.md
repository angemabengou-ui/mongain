# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code — this project runs Expo SDK 54 (see package.json), not v57. Update this line if the app is upgraded.
